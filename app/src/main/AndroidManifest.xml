<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!--
    ========================================================================
    Aurora IME - 绝对零网络权限清单
    ========================================================================
    设计原则: 不声明任何 INTERNET / ACCESS_NETWORK_STATE 权限
    tools:node="remove" 用于强制剥离第三方依赖可能注入的网络权限
    ========================================================================
    -->

    <!-- 必需: 麦克风 (语音输入) -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- 可选: 前台服务,语音识别保活 -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />

    <!-- ⭐ 核武: 强制移除任何依赖可能注入的网络权限 -->
    <uses-permission android:name="android.permission.INTERNET" tools:node="remove" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" tools:node="remove" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" tools:node="remove" />
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" tools:node="remove" />

    <application
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/Theme.AuroraIME"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="false"
        android:supportsRtl="true"
        tools:targetApi="33">

        <!-- IME 服务 -->
        <service
            android:name=".ime.AuroraInputMethodService"
            android:permission="android.permission.BIND_INPUT_METHOD"
            android:exported="true">
            <intent-filter>
                <action android:name="android.view.InputMethod" />
            </intent-filter>
            <meta-data
                android:name="android.view.im"
                android:resource="@xml/method" />
        </service>

        <!-- 设置 Activity (可选) -->
        <activity
            android:name=".ime.SettingsActivity"
            android:exported="false"
            android:label="@string/settings_title">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
            </intent-filter>
        </activity>

    </application>
</manifest>